//your JS code here. If required.

function compareTwo(n,m){
	if(n === m) return true;
	else false;
}

const n = parseInt(prompt("Enter number: "));
const m = parseInt(prompt("Enter number: "));

alert(compareTwo(n,m));